<h1 class= "text-center mb-5">名所一覧</h1>
<ul class="list-unstyled">
  <% @spots.each do |spot| %>
    <li class="media mb-3">
        <div class="media-body">
             <%= link_to spot.name, spot_path(spot) %>
        </div>
    </li>
  <% end %>
  <%= paginate @spots %>
</ul>